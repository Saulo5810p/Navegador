<?xml version="1.0" encoding="utf-8"?>
<!-- Fundo do widget de busca 4x1 (estilo Chromium): mesma cápsula com
     cantos totalmente arredondados usada no campo de busca da Home
     (bg_search_field.xml), só que um pouco mais "alta" de raio pra
     acompanhar a altura maior da célula do widget. Resolvido junto com a
     variante -night automaticamente pelo sistema de recursos, sem
     precisar de código extra. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFFFF" />
    <corners android:radius="32dp" />
    <stroke android:width="1dp" android:color="#33000000" />
</shape>
