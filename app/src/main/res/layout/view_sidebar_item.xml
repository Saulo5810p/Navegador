<?xml version="1.0" encoding="utf-8"?>
<!-- Item de atalho de FUNÇÃO na sidebar (Downloads, Configurações,
     Histórico, Desktop/Mobile) - ícone + rótulo lado a lado, diferente
     do atalho de SITE (view_shortcut_item.xml), que é vertical com
     inicial em círculo. Layout horizontal porque a sidebar é uma coluna
     estreita - cada função ocupa uma linha própria. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sidebarItemRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <ImageView
        android:id="@+id/sidebarItemIcon"
        android:layout_width="22dp"
        android:layout_height="22dp" />

    <TextView
        android:id="@+id/sidebarItemLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorPrimary" />
</LinearLayout>
