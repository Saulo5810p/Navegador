<?xml version="1.0" encoding="utf-8"?>
<!--
    Item de histórico: círculo com a inicial do site (mesmo padrão do
    view_shortcut_item.xml, reaproveitado aqui) + barra retangular com
    fundo GradientShadowCardView (nome em cima, URL recortada embaixo).
    Toda a linha é clicável e abre o site na WebView.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?android:attr/selectableItemBackground">

    <FrameLayout
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_shortcut_add_button">

        <TextView
            android:id="@+id/historyInitial"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textSize="16sp"
            android:textColor="?android:attr/textColorPrimary" />
    </FrameLayout>

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_weight="1"
        android:layout_marginStart="12dp">

        <com.xaulinxs.aosp.browser.widget.GradientShadowCardView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:cornerRadiusDp="14" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:paddingStart="14dp"
            android:paddingEnd="14dp">

            <TextView
                android:id="@+id/historyItemTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="?android:attr/textColorPrimary"
                android:singleLine="true"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/historyItemUrl"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="?android:attr/textColorSecondary"
                android:singleLine="true"
                android:ellipsize="middle" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
